# 壁表示機能の実装プラン

## 1. 概要

既存の `RoadNetwork.jsx` の仕組みを基盤として、タイルマップデータから壁を自動的に抽出し、3Dシーン上に配置する新しいコンポーネント `WallNetwork.jsx` を作成します。このコンポーネントは、隣接する壁タイルの配置関係を解析し、「直線」「角」「T字」「十字」「終端」といった壁のタイプを自動的に判定します。判定結果に基づき、将来的には対応するGLTFモデルを、当面はデバッグ用のボックスメッシュを適切な向きで配置します。

## 2. 実装ステップ

### Step 1: `layout.js` の拡張
- **タイルタイプの追加:** `TILE_TYPES` オブジェクトに `WALL: 2` のように、壁を表す新しい定数を追加します。
- **マップデータの更新:** `sampleMapData` の `tiles` 配列に、壁を配置します（`2` を使用）。これにより、壁の生成ロジックをテストするための基本的なレイアウトを定義します。

### Step 2: `WallNetwork.jsx` の作成
- **ファイルの複製:** `reflence/wall/RoadNetwork.jsx` を `src/WallNetwork.jsx` として複製します。
- **コンポーネントの改名:** ファイル内のコンポーネント名を `RoadNetwork` から `WallNetwork` に変更します。
- **ロジックの修正:**
    - 道路タイルを検出するロジックを、`TILE_TYPES.ROAD` から `TILE_TYPES.WALL` を参照するように変更します。
    - 道路タイプを判定する関数 `getRoadType` を、壁用に `getWallType` という名前で複製・修正します。チェック対象を壁タイルに変更します。
    - 回転角を計算する `getRoadRotation` は、ロジックが共通であるため `getWallRotation` として流用します。

### Step 3: レンダリングロジックの適応
- **フォールバック描画:** 当初はGLTFモデルの代わりに、常にボックスメッシュを描画するようにします。これは `RoadNetwork.jsx` の `useBoxRoads` フラグを `true` に設定するのと同等の実装です。
- **デバッグ色の設定:** `getColorByType` 関数を壁用に調整し、壁のタイプ（直線、角など）に応じて異なる色でボックスが表示されるようにします。これにより、タイプ判定ロジックが正しく機能しているかを視覚的に確認できます。
- **将来的な拡張:** 道路モデルと同様の `assets/walls/wall_straight.glb` のような命名規則で、将来的にはGLTFモデルを読み込めるように設計しておきます。

### Step 4: `Scene.jsx` への統合
- **コンポーネントの追加:** 作成した `WallNetwork.jsx` コンポーネントを `Scene.jsx` にインポートします。
- **レンダリング:** `Scene` コンポーネント内で `<WallNetwork />` を追加し、`mapData` をプロパティとして渡します。これにより、壁が3Dシーンに表示されるようになります。

## 3. ファイル構成の変更

- **新規作成:**
  - `src/WallNetwork.jsx`
  - `plan.md`
- **変更:**
  - `src/layout.js` （タイルタイプの定義とマップデータの更新）
  - `src/Scene.jsx` （`WallNetwork` コンポーネントのインポートと配置）
