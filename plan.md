# plan.md

## 課題
シミュレーションのステップ間のエージェントの移動が離散的で、アニメーションがカクカクして見える。

## 解決策
各ステップ間のエージェントの位置を補間することで、滑らかな移動アニメーションを実現する。

## 提案手法
`@react-three/fiber` の `useFrame` フックを利用して、各フレームでエージェントの表示位置を更新する手動補間を実装する。この方法は、新たなライブラリを追加することなく実現できる。

### 実装計画
1.  **エージェントの状態管理の変更**:
    *   `Scene.jsx` で、エージェントの現在の位置だけでなく、1つ前のステップの位置も保持するように状態管理を修正する。
    *   シミュレーションが1ステップ進むごとに、前の位置と新しい位置を更新する。

2.  **`AgentMen` コンポーネントの修正**:
    *   `AgentMen` コンポーネントを、個々の `MenModel` を管理する `AgentMan` コンポーネント（単数形）に分割する。
    *   `AgentMan` コンポーネント内で `useFrame` を使用する。

3.  **`useFrame` での補間処理**:
    *   `AgentMan` コンポーネント内で、`useFrame` を使い、毎フレーム、エージェントのメッシュの `position` を更新する。
    *   `three.js` の `Vector3.lerp()` メソッドを使い、前の位置から新しい位置へ向かって少しずつ移動させる（線形補間）。
    *   回転（`rotation`）についても同様に `quaternion.slerp()` を使って球面線形補間を行い、向きの変更を滑らかにする。

4.  **コードの構造**:
    *   `Scene.jsx` 内に `AgentMan` コンポーネントを新しく作成する。
    *   `AgentMen` コンポーネントは、`agents` 配列を `map` して、各エージェントに対応する `AgentMan` コンポーネントをレンダリングするように変更する。

### このアプローチの利点
*   **既存の依存関係のみ使用**: `@react-three/fiber` はすでにプロジェクトに導入されているため、新たなライブラリを追加する必要がない。
*   **完全な制御**: アニメーションの速度やイージングなどを細かく調整できる。

### 代替案（今回は採用しない）
*   **`react-spring` の利用**:
    *   より宣言的にアニメーションを記述できるが、新しい依存関係（`@react-spring/three`）を追加する必要がある。
    *   今回は、既存のツールで解決できるため、依存関係を増やさない方法を優先する。
