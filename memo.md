# `agentScript.js` におけるエージェントの `theta` (向き) のロジック解析

このドキュメントは、シミュレーションにおけるエージェントの `theta` プロパティがどのように決定されるかのロジックをまとめたものです。

## 1. `theta` の決定ロジック

エージェント（スクリプト内では "turtle" と呼ばれる）の `theta` プロパティは、エージェントの向きや進行方向を表します。この値は `ExitModel` クラスの `step()` メソッド内で設定されます。

中心となるコードは `t.face(min)` です。

- **`t`**: 個々のエージェント（turtle）を表します。
- **`min`**: エージェントが現在のステップで移動する目標のパッチ（グリッドセル）を表します。

ロジックは以下の通りです。
1.  各エージェントは、隣接するパッチの中から、壁ではなく、かつ他のエージェントがいないセルを特定します。
2.  利用可能な隣接パッチの中から、自身に割り当てられた出口 (`t.exit`) に最も近いパッチ (`min`) を選択します。
3.  エージェントは、この新しいパッチが出口に現在位置よりも近い場合にのみ移動します。
4.  移動する前に、エージェントは `t.face(min)` を呼び出します。これにより、エージェントは目標のパッチ `min` の中心を向くように方向転換します。この操作により、エージェントの現在位置から目標位置までの角度が計算され、エージェントの `theta` プロパティがその新しい値に設定されます。

## 2. `theta` の単位

AgentScriptのドキュメントと慣例に基づき、`theta` の値の単位は **度 (degrees)** です。

- `0` 度は「北」（2Dシミュレーショングリッドの正のY軸、3D可視化では正のZ軸に相当）を指します。
- 角度は時計回りに増加します。例えば、`90` 度は「東」（正のX軸）を指します。

この仕様は、`Scene.jsx` において `theta` が `three.js` で使用するために度からラジアンに変換されている実装と一致しています。
